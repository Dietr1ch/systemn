{ ... }:

# TODO: Set up ACME
# https://nixos.wiki/wiki/ACME
{
  security = {
    # https://search.nixos.org/options?channel=unstable&query=security.acme
    acme = {
      # acceptTerms = true;
      # defaults = {
      # };
    };
  };
}
